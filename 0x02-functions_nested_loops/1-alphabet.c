#include "main.h"

/**
 * print_alphabet - check the code
 *
 * Return: Always 0 (success)
 */

void print_alphabet(void)
{
	char id;

	for (id = 'a'; id <= 'z'; id++)
	{
		_putchar(id);
	}
	_putchar('\n');
}

